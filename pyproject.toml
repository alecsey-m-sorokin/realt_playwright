[project]
name = "realt-playwright"
version = "0.1.0"
description = ""
authors = [
    {name = "alesorokin",email = "alesorokin@dengisrasy.ru"}
]
readme = "README.md"
requires-python = ">=3.12, <4.0"
dependencies = [
    "playwright (>=1.57.0,<2.0.0)",
    "pytest (>=9.0.2,<10.0.0)",
    "pytest-playwright (>=0.7.2,<0.8.0)",
    "loguru (>=0.7.3,<0.8.0)",
    "screeninfo (>=0.8.1,<0.9.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.pytest.ini_options]
